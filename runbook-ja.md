# 項目一括追加ツール 手順書

対象: 顧客環境の「項目編集」画面。インストール作業は不要です。

## 準備

追加する項目の一覧（Excel）を開いておく。**見出し行ごとコピーするのがおすすめです。**
列は名前で判定するので、順番は自由です。No. 列があっても問題ありません。
見出し行が無い場合も中身から列を判定しますが、**幅と説明の列は読めません**
（見出し行が必要です）。解析後に出る「形式の判定: …」の行で、
どの列がどう読まれたかを必ず確認してください。

| No. | 項目名 | 項目タイプ | 紐づけ先レコード | 選択肢 | | |
|---|---|---|---|---|---|---|
| 1 | 物件　情報 | 見出し | | | | |
| 2 | 依頼書ID | ※システム項目 | | | | |
| 3 | 物件名 | テキスト | | | | |
| 4 | 工事担当 | 紐付け | 業者 | | | |
| 5 | 確認済証交付者 | プルダウン | | 第一検査機関 | 第二検査機関 | 第三検査機関 |
| | | | | 第四検査機関 | | |

- **`※システム項目` の行は自動で飛ばします。** eSM が最初から作る項目なので、
  追加しようとすると二重になります。
- **選択肢は何列に分かれていても構いません。** 1行に収まらず次の行に続いている
  場合（上の例の 第四検査機関）も、項目名と項目タイプが空なら前の行の続きとして
  読みます。
- **`（仮）` も選択肢として登録します。** 後で本番の選択肢に直してください。
- 必須列があれば `○` で必須になります。
- **紐づけ先レコード** は `業者` のような表示名でも `sheet_134` でも構いません。
- `※小数点以下2桁` `※後ろに単位「号」` `※初期値本日` の3つだけは設定として
  反映します。それ以外の `※` の書き込みは**ログに出すだけで何もしません**。
  必要な設定は後で手で入れてください。
- 演算（文字）・演算（数値） は**このツールでは追加できません**（手動）。

### 紐づけ項目を追加するときの注意

紐づけ項目を1つ追加すると、**紐づけ先のシートにも逆側の項目が1つ増えます**
（例: 依頼書に「基礎（業者）」を作ると、業者シートに「基礎（依頼書）」ができます）。
これは eSM で手作業で紐づけ項目を作ったときとまったく同じ動作ですが、
画面に出ていないシートが変わるので、実行前の確認画面に件数を表示します。

**そのシートへの紐づけ項目が1つも無い場合は追加できません。**
最初の1件だけ画面から手で作ってください。2件目以降はツールが追加できます。

### 紐づけ参照を追加するときの注意

紐づけ参照は**それ自体は項目になりません**。紐づけ先シートの項目を、
紐づけ項目を通して表示するものです。書き方は次のとおりです。

| 項目名 | 項目タイプ | 紐づけ先レコード |
|---|---|---|
| 契約支店 | 紐付け参照 | 支店＋工事担当 |
| 工事担当 | 紐付け | 支店＋工事担当 |

- **項目名には、紐づけ先シートにある項目の名前をそのまま書いてください。**
  紐づけ参照は紐づけ先の項目名がそのまま表示されるためです。
  紐づけ先にその名前の項目が無い場合は、ドライランでその行だけ止まります。
- **通す紐づけ項目の行を、同じ項目リストに入れてください。**
  既にある紐づけ項目には追加できません（既存項目には一切手を触れない作りのため）。
  上の例なら「工事担当」の行が必要で、契約支店はその工事担当を通して表示されます。
- ドライランに **どの紐づけ項目を通すか** を1行ずつ表示します。実行前に確認してください。
- 同じシートへの紐づけ項目が2つある項目リストでは、どちらを通すか決められないため止まります。

## 手順

1. 対象シートの **項目編集** 画面を開く。
2. `F12` → **Console** タブ。
3. `esm-layout-tool.js` の中身を全部貼り付けて `Enter`。右上にパネルが出ます。
   パネルは**上部のバーをドラッグして移動**できます（位置は次回も覚えています）。
   画面外に行ってしまったときは、コンソールで
   `__esmLayoutTool.S.resetPosition()` と入力すると右上に戻ります。
4. Excel から行をコピーし、パネルのテキスト欄に貼り付ける。
   必要なら「1行に並べる数」を選ぶ（初期値は4）。
5. **一括で実行** を押す（型ごとの成否を確かめたいときは **1件ずつ実行**）。
   - **一括**: 1回のリクエストでまとめて追加します。全部成功か、全部失敗か。
   - **1件ずつ**: 1項目ずつ追加し、失敗した項目は飛ばして続行します。
     どの型が通ってどの型が落ちたかが結果に出ます。初めてのシートではこちらが安全です。
   - 最初に確認ダイアログが出て、画面の「保存」が1回押されます。
     **画面上に未保存の編集が残っていないことを必ず確認してから OK を押してください。**
     残っていると、その編集も一緒に保存されます。
   - 追加内容の一覧が出て、確認ダイアログが表示されます。
     一覧には貼り付けた**全部の行**が出ます（追加 / スキップ / 追加不可）。
     「追加不可」の行は理由が書いてあります。手動で作ってください。
     追加できない行があっても、残りの行はそのまま実行できます。
   - 実行前に現在の状態が `esm-layout-before-*.json` としてダウンロードされます。
     **確認が終わるまでは消さないでください**（切り戻し用）。
     顧客環境のダウンロードフォルダに残るので、**確認後は削除してください**。
   - 1件ずつ実行は1項目あたり数秒かかります。件数が多いときは数分かかるので、
     確認ダイアログに出る目安時間を見てから開始してください。途中で画面を閉じないでください。
6. 完了すると実行結果がクリップボードにコピーされます。担当者に送ってください。
7. **続けて別の項目を追加する場合**: そのまま次の行を貼り付けて実行するだけです。
   画面の再読み込みは不要です。
8. すべて終わったら画面を再読み込みし、項目が追加されていることを確認する。

## 実行前の確認

- 項目編集画面を**開いた直後**であること（手で何も変更していないこと）
  - 未保存の変更が残っていると、テンプレート取得時の「保存」で一緒に保存されます。
    その場合は「⚠ 画面に未保存の項目が N 件あり、いま一緒に保存されました」と表示されるので、
    意図した変更かどうか確認してください。
- 他の人が同じシートを同時に編集していないこと
- 対象シートが合っていること（画面のシートと違う場合は自動で中止します）

## 注意

- 項目は画面の**一番下**、**新しい行から**追加されます。
  1行に並べる数はパネルの「1行に並べる数」で選べます（1／2／3／4、初期値4）。
  - **1**: 1項目で横幅いっぱい
  - **2**: 2項目で1行（各項目は横幅の半分）
  - **3**: 3項目で1行（4つ目のマスは空きになります）
  - **4**: 4項目で1行（標準の見た目）
  eSM の項目編集画面は 1行 = 4マス（項目の幅は 25% 刻み）が上限です。
  5以上は指定できません（指定しても eSM 側で4つずつ折り返されます）。
  見出しは設定にかかわらず必ず1行を丸ごと使います。
  Excel に「幅」の列がある場合は、その行だけ列の値が優先されます。
  選んだ数は次回も覚えています。並び順の調整やタブへの移動は手作業です。
- 同じ名前の項目が既にある行は自動でスキップされます（二重登録されません）。
- **項目リストの中で名前が重複している行**もスキップし、ログに出します。
  （実際の指示書には「解体」「トラス」のように、別の型で同じ名前が2回出てくることがあります。
  どちらを使うか決めて、片方の名前を変えてから実行してください。）
- 「⚠未検証」と出た型は、まず1件だけで試してください。
- 件数が多いとき（100件以上）は **1件ずつ実行** をおすすめします。
  途中で失敗しても、成功した分はそのまま残り、もう一度実行すれば失敗した分だけ再試行されます。

## うまくいかないとき

- 「他の人が … を更新しています」→ 画面を再読み込みして手順1からやり直す。
  1件ずつ実行の途中で出た場合は、そこで自動的に止まります。追加済みの分はそのまま残るので、
  画面を再読み込みしてもう一度実行すれば、残りだけが追加されます。
- 「タイムアウト(120秒)」→ **再送はされません。** 画面を再読み込みして、
  その項目が追加されているかどうかを目で確認してから続けてください。
- 「実行中です。終わるまでお待ちください」→ 前の処理がまだ動いています。ボタンを続けて
  押しても二重には実行されません（安全側で止めています）。ログが止まるまで待ってください。
- 「計画が古くなっています」→ 途中で別の保存が入った場合に出ます。
  ドライランからやり直してください（安全側で止めています）。
  紐づけ先のシート名が出た場合は、そのシートを誰かが編集しています。
- 「紐づけ先『◯◯』がこのシートの既存の紐づけ項目に見つかりません」→
  そのシートへの紐づけ項目を1件だけ画面から手で作り、もう一度実行する。
- 「参照元の紐づけ項目が同じ項目リストにありません」→ 紐づけ参照の行だけでは追加できません。
  通す紐づけ項目の行を同じリストに足してください。
- 「参照元シート ◯◯ に『△△』という項目がありません」→ 項目名が紐づけ先シートの
  項目名と一致していません。紐づけ先シートで正しい名前を確認してください。
- 赤い文字のエラーが出た → **それ以上操作せず**、ログの内容と
  `esm-layout-before-*.json` を担当者に送る。
- 「保存リクエストを取得できませんでした」→ 画面上の **保存** を手で1回押してから、
  もう一度 **実行** を押す。
