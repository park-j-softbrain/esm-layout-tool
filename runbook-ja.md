# 項目一括追加ツール 手順書

対象: 顧客環境の「項目編集」画面。インストール作業は不要です。

## 準備

追加する項目の一覧（Excel）を開いておく。列は次の順（**型と項目名はどちらが先でも可**）:

| 型 | 項目名 | 必須 | 選択肢 | 幅 | 説明 |
|---|---|---|---|---|---|
| テキスト | 担当者名 | ○ | | | |
| プルダウン | ステータス | | 未対応,対応中,完了 | | |

- 必須: `○` を入れる（空欄なら任意項目）
- 選択肢: プルダウン / ラジオボタン / チェックボックス のみ。カンマ区切り
- 演算（文字）・演算（数値）・紐づけ項目 は**このツールでは追加できません**（手動）

## 手順

1. 対象シートの **項目編集** 画面を開く。
2. `F12` → **Console** タブ。
3. `esm-layout-tool.js` の中身を全部貼り付けて `Enter`。右上にパネルが出ます。
4. Excel から行をコピーし、パネルのテキスト欄に貼り付ける。
5. **一括で実行** を押す（型ごとの成否を確かめたいときは **1件ずつ実行**）。
   - **一括**: 1回のリクエストでまとめて追加します。全部成功か、全部失敗か。
   - **1件ずつ**: 1項目ずつ追加し、失敗した項目は飛ばして続行します。
     どの型が通ってどの型が落ちたかが結果に出ます。初めてのシートではこちらが安全です。
   - 最初に確認ダイアログが出て、画面の「保存」が1回押されます。
     **画面上に未保存の編集が残っていないことを必ず確認してから OK を押してください。**
     残っていると、その編集も一緒に保存されます。
   - 追加内容の一覧が出て、確認ダイアログが表示されます
   - 実行前に現在の状態が `esm-layout-before-*.json` としてダウンロードされます。
     **このファイルは消さないでください**（切り戻し用）
6. 完了すると実行結果がクリップボードにコピーされます。担当者に送ってください。
7. **続けて別の項目を追加する場合**: そのまま次の行を貼り付けて実行するだけです。
   画面の再読み込みは不要です。
8. すべて終わったら画面を再読み込みし、項目が追加されていることを確認する。

## 実行前の確認

- 項目編集画面を**開いた直後**であること（手で何も変更していないこと）
- 他の人が同じシートを同時に編集していないこと
- 対象シートが合っていること（画面のシートと違う場合は自動で中止します）

## 注意

- 項目は画面の**一番下**にまとめて追加されます。並び順やタブの移動は手作業です。
- 同じ名前の項目が既にある行は自動でスキップされます（二重登録されません）。
- 「⚠未検証」と出た型は、まず1件だけで試してください。

## うまくいかないとき

- 「他の人がこのシートを更新しています」→ 画面を再読み込みして手順1からやり直す。
- 赤い文字のエラーが出た → **それ以上操作せず**、ログの内容と
  `esm-layout-before-*.json` を担当者に送る。
- 「保存リクエストを取得できませんでした」→ 画面上の **保存** を手で1回押してから、
  もう一度 **実行** を押す。
